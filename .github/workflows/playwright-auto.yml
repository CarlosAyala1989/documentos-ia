name: CI - Playwright Automático

on:
  push:
    branches:
      - main         # Cambia esto si tu rama principal es otra

permissions:
  contents: read
  actions: write

jobs:
  run-playwright-tests:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Clonar repositorio
        uses: actions/checkout@v3

      - name: 🟢 Configurar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Instalar dependencias
        run: npm install

      - name: 🌐 Instalar navegadores de Playwright
        run: npx playwright install --with-deps

      - name: 🧪 Ejecutar todos los tests con reporter y extracción de videos
        run: npm run test:reporte

      - name: 📁 Subir reportes como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: reporte-evidencia
          path: reportes/
